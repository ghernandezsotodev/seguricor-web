---
import Layout from '../layouts/Layout.astro';
// NO hay import de 'form.html?raw'. Ha sido eliminado.
---

<Layout title="Contacto - Seguricor">
  <main class="flex-1">
    <div class="container mx-auto px-4 py-16 md:py-24">
      <div class="text-center max-w-2xl mx-auto mb-12">
        <h1 class="text-5xl font-heading font-bold text-neutral-900">
          Contáctanos
        </h1>
        <p class="text-lg text-neutral-500 mt-2">
          ¿Listo para comenzar tu próximo proyecto? Envíanos un mensaje.
        </p>
      </div>
      <div class="max-w-4xl mx-auto md:flex md:gap-16">
        <div class="md:w-2/3">

          {/* === INICIO DEL FORMULARIO REAL === 
            Este formulario HTML es normal. Netlify lo interceptará
            porque el archivo 'public/form.html' (el "cebo")
            ya activó la detección del formulario "contact".
          */}
          <form 
            name="contact" 
            method="POST" 
            action="/gracias"
            class="space-y-4"
          >
            <input type="hidden" name="form-name" value="contact" />

            <p class="hidden">
              <label>
                No llenar si eres humano: <input name="bot-field" />
              </label>
            </p>

            <div>
              <label for="name" class="block text-sm font-medium text-neutral-700 mb-1">
                Nombre Completo
              </label>
              <input
                type="text"
                id="name"
                name="name"
                class="w-full bg-neutral-100 border-2 border-neutral-100 rounded-md p-3
                       focus:border-primary-600 focus:ring-0 outline-none"
                required
              >
            </div>
            <div>
              <label for="email" class="block text-sm font-medium text-neutral-700 mb-1">
                Email
              </label>
              <input
                type="email"
                id="email"
                name="email"
                class="w-full bg-neutral-100 border-2 border-neutral-100 rounded-md p-3
                       focus:border-primary-600 focus:ring-0 outline-none"
                required
              >
            </div>
            <div>
              <label for="message" class="block text-sm font-medium text-neutral-700 mb-1">
                Mensaje
              </label>
              <textarea
                id="message"
                name="message"
                rows="5"
                class="w-full bg-neutral-100 border-2 border-neutral-100 rounded-md p-3
                       focus:border-primary-600 focus:ring-0 outline-none"
                required
              ></textarea>
            </div>
            <div>
              <button
                type="submit"
                class="mt-2 w-full bg-primary-600 text-white font-bold py-3 px-6 rounded-lg font-heading
                       hover:bg-primary-500 transition-colors"
              >
                ENVIAR MENSAJE
              </button>
            </div>
          </form>
          {/* === FIN DEL FORMULARIO REAL === */}

        </div>
        <div class="md:w-1/3 mt-12 md:mt-0">
          <h3 class="text-2xl font-heading font-medium text-neutral-900 mb-4">
            Información Directa
          </h3>
          <div class="space-y-3 text-neutral-700">
            <p>
              <strong>Email:</strong><br>
              <a href="mailto:info@seguricor.com" class="hover:text-primary-600">
                info.seguricor@gmail.com
              </a>
            </p>
            <p>
              <strong>Teléfono:</strong><br>
              <a href="tel:+56971907925" class="hover:text-primary-600">
                +56 9 71907925
              </a>
            </p>
            <p>
              <strong>Ubicación:</strong><br>
              Talca, Región del Maule
            </p>
          </div>
        </div>
      </div>
    </div>
  </main>

  {/* El div 'set:html' ha sido eliminado.
    El archivo 'public/form.html' sigue existiendo y Netlify lo encontrará.
  */}

</Layout>